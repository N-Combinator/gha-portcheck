name: uses-url
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: https://github.com/actions/upload-artifact@v4
      - uses: github.com/actions/download-artifact@v4
      - uses: https://code.forgejo.org/actions/upload-artifact@v4

name: b-artifacts
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: actions/download-artifact@v4
        with:
          name: dist
